# name: Auth Service CI/CD - Test Branch

# on:
#   pull_request:
#     branches:
#       - test
#     paths:
#       - "auth/**"

# jobs:
#   build-and-test:
#     name: Build & Test (Spring Boot)
#     runs-on: ubuntu-latest
#     defaults:
#       run:
#         working-directory: auth
#     env:
#       DB_URL: jdbc:h2:mem:testdb
#       DB_USER: test-user
#       DB_PASSWORD: test-pass
#       KAFKA_BROKERS: localhost:9092
#       JWT_KEY: test-jwt-key
#     steps:
#       - name: Checkout code
#         uses: actions/checkout@v4

#       - name: Set up JDK 21
#         uses: actions/setup-java@v4
#         with:
#           distribution: temurin
#           java-version: "21"
#           cache: maven

#       - name: Build & Run Tests
#         run: mvn clean verify -B

#       - name: Archive Test Reports
#         if: always()
#         uses: actions/upload-artifact@v4
#         with:
#           name: auth-test-reports
#           path: auth/target/surefire-reports/

#       - name: Package JAR
#         run: mvn -B package

#       - name: Upload JAR Artifact
#         uses: actions/upload-artifact@v4
#         with:
#           name: auth-jar
#           path: auth/target/*.jar

#   # docker-build-push:
#   #   name: Docker Build & Push
#   #   runs-on: ubuntu-latest
#   #   needs: build-and-test
#   #   steps:
#   #     - name: Checkout code
#   #       uses: actions/checkout@v4

#   #     - name: Set up Docker Buildx
#   #       uses: docker/setup-buildx-action@v3

#   #     - name: Login to Docker Hub
#   #       uses: docker/login-action@v3
#   #       with:
#   #         username: ${{ secrets.DOCKER_USERNAME }}
#   #         password: ${{ secrets.DOCKER_PASSWORD }}

#   #     - name: Extract short SHA
#   #       id: vars
#   #       run: echo "sha=$(git rev-parse --short HEAD)" >> $GITHUB_OUTPUT

#   #     - name: Download JAR Artifact
#   #       uses: actions/download-artifact@v4
#   #       with:
#   #         name: auth-jar
#   #         path: auth/target/

#   #     - name: Move JAR to target directory
#   #       run: |
#   #         find auth/target -type f -name "*.jar" -exec mv {} auth/target/ \;

#   #     - name: Build & Push Docker Image
#   #       uses: docker/build-push-action@v5
#   #       with:
#   #         context: auth
#   #         file: auth/Dockerfile
#   #         push: true
#   #         tags: |
#   #           rajanbansal553/rajan-repo:auth-test-latest
#   #           rajanbansal553/rajan-repo:auth-test-${{ steps.vars.outputs.sha }}
#   # deploy-to-k8s:
#   #   name: Deploy to Kubernetes (Test Env)
#   #   runs-on: ubuntu-latest
#   #   needs: docker-build-push
#   #   steps:
#   #     - name: Set up Kubeconfig
#   #       run: |
#   #         echo "${{ secrets.KUBE_CONFIG_DATA }}" | base64 -d > $HOME/.kube/config

#   #     - name: Extract short SHA again
#   #       id: vars
#   #       run: echo "sha=$(git rev-parse --short HEAD)" >> $GITHUB_OUTPUT

#   #     - name: Patch Deployment Image
#   #       run: |
#   #         kubectl set image deployment/auth auth-container=rajanbansal553/rajan-repo:auth-test-${{ steps.vars.outputs.sha }} --namespace=test

#   #     - name: Verify Pod Health
#   #       run: |
#   #         kubectl rollout status deployment/auth --namespace=test
