# name: Payment Service CI/CD - Test Branch

# on:
#   pull_request:
#     branches:
#       - test
#     paths:
#       - "payment/**"

# jobs:
#   build-and-test:
#     name: Build & Test (Spring Boot)
#     runs-on: ubuntu-latest
#     defaults:
#       run:
#         working-directory: payment
#     env:
#       DB_URL: jdbc:h2:mem:testdb
#       DB_USER: test-user
#       DB_PASSWORD: test-pass
#       KAFKA_BROKERS: localhost:9092
#       JWT_KEY: test-jwt-key
#     steps:
#       - name: Checkout code
#         uses: actions/checkout@v4

#       - name: Set up JDK 21
#         uses: actions/setup-java@v4
#         with:
#           distribution: temurin
#           java-version: "21"
#           cache: maven

#       - name: Build & Run Tests
#         run: mvn clean verify -B

#       - name: Archive Test Reports
#         if: always()
#         uses: actions/upload-artifact@v4
#         with:
#           name: payment-test-reports
#           path: payment/target/surefire-reports/

#       - name: Package JAR
#         run: mvn -B package

#       - name: Upload JAR Artifact
#         uses: actions/upload-artifact@v4
#         with:
#           name: payment-jar
#           path: payment/target/*.jar

#   # docker-build-push:
#   #   name: Docker Build & Push
#   #   runs-on: ubuntu-latest
#   #   needs: build-and-test
#   #   steps:
#   #     - name: Checkout code
#   #       uses: actions/checkout@v4

#   #     - name: Set up Docker Buildx
#   #       uses: docker/setup-buildx-action@v3

#   #     - name: Login to Docker Hub
#   #       uses: docker/login-action@v3
#   #       with:
#   #         username: ${{ secrets.DOCKER_USERNAME }}
#   #         password: ${{ secrets.DOCKER_PASSWORD }}

#   #     - name: Extract short SHA
#   #       id: vars
#   #       run: echo "sha=$(git rev-parse --short HEAD)" >> $GITHUB_OUTPUT

#   #     - name: Download JAR Artifact
#   #       uses: actions/download-artifact@v4
#   #       with:
#   #         name: payment-jar
#   #         path: payment/target/

#   #     - name: Move JAR to target directory
#   #       run: |
#   #         find payment/target -type f -name "*.jar" -exec mv {} payment/target/ \;

#   #     - name: Build & Push Docker Image
#   #       uses: docker/build-push-action@v5
#   #       with:
#   #         context: payment
#   #         file: payment/Dockerfile
#   #         push: true
#   #         tags: |
#   #           rajanbansal553/rajan-repo:payment-test-latest
#   #           rajanbansal553/rajan-repo:payment-test-${{ steps.vars.outputs.sha }}
#   # deploy-to-k8s:
#   #   name: Deploy to Kubernetes (Test Env)
#   #   runs-on: ubuntu-latest
#   #   needs: docker-build-push
#   #   steps:
#   #     - name: Set up Kubeconfig
#   #       run: |
#   #         echo "${{ secrets.KUBE_CONFIG_DATA }}" | base64 -d > $HOME/.kube/config

#   #     - name: Extract short SHA again
#   #       id: vars
#   #       run: echo "sha=$(git rev-parse --short HEAD)" >> $GITHUB_OUTPUT

#   #     - name: Patch Deployment Image
#   #       run: |
#   #         kubectl set image deployment/payment payment-container=rajanbansal553/rajan-repo:payment-test-${{ steps.vars.outputs.sha }} --namespace=test

#   #     - name: Verify Pod Health
#   #       run: |
#   #         kubectl rollout status deployment/payment --namespace=test
